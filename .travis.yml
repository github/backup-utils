---
script:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then make test; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then debuild -uc -us; fi

addons:
  apt:
    packages:
      - devscripts
os:
  - linux
  - osx
